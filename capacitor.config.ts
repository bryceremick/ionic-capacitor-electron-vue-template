import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-capacitor-electron-vue-template',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
